<%= form_for @institution_list, :html => { :class => 'form-horizontal', :role => 'form' } do |f| %>
  <% if @institution_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@institution_list.errors.count, "error") %> prohibited this institution_list from being saved:</h2>

      <ul>
      <% @institution_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="col-lg-4">
  <div class="well">
    <div class="row">
      <div class="col-lg-12">
        <fieldset>
          <div class="form-group">
            <%= f.label :sigla %><br>
            <%= f.text_field :sigla, :class=>'form-control input-sm' %>
          </div>
          <div class="form-group">
            <%= f.label :nombre %><br>
            <%= f.text_field :nombre, :class=>'form-control input-sm' %>
          </div>
          <div class="form-group">
            <input type="submit" value="Registrar" class="btn btn-primary">
            <a href="<%=institution_lists_path%>" class="btn btn-default btn-sm active" role="button"> Atras </a>
          </div>
        </fieldset>
      </div>
    </div>
  </div>
</div>
<% end %>