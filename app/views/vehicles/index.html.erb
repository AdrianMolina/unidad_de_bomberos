<br>
<br>
<div class="col-lg-12">
  <div class="page-header">
       <h1 id="forms">Lista de Vehiculos</h1>
  </div>
</div>

<div class="col-lg-8">
  <div class="well">
    <div class="row">
      <div class="col-lg-12">
        <table class="table table-bordered">
          <thead>
            <tr>
              <th>Sigla</th>
              <th>Placa</th>
              <th>Modelo</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @vehicles.each do |vehicle| %>
              <tr>
                <td><%= vehicle.sigla %></td>
                <td><%= vehicle.placa %></td>
                <td><%= vehicle.modelo %></td>
                <td align="center"><%= link_to 'Show', vehicle %> |
                    <%= link_to 'Edit', edit_vehicle_path(vehicle) %> |
                    <%= link_to 'Destroy', vehicle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
  <a href="<%=new_vehicle_path%>" class="btn btn-primary btn-sm active" role="button">Registrar Nuevo vehiculo</a>
</div>